# This file tells Git which files and folders to ignore.
# It helps keep system files, editor settings, and other local clutter
# out of version control and off of GitHub.

# macOS system files
.DS_Store

# VS Code settings folder
.vscode/

# Dependency folders (e.g., if tooling is added later)
node_modules/
